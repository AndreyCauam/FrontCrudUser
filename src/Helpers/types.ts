export interface User {
    id?: number;
    name: string;
    email: string;
    birthdate: Date;
}

export interface Notification {
    title: string;
    message: string;
    type: string;
}

export interface Variable {
    name: string;
    email: string;
    birthdate: Date;
}